langcode: en
status: true
dependencies:
  config:
    - elasticsearch_connector.cluster.elasticsearch
    - key.key.elastic_password
id: elastic_password
label: 'Elastic password'
config_type: elasticsearch_cluster
config_prefix: elasticsearch_connector.cluster
config_name: elasticsearch
config_item: options.password
key_id: elastic_password
